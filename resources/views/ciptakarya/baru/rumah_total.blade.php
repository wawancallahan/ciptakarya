<div class="form-group">
    <label for="">Harga Rumah (m^2)</label>
    <p class="form-control">{{ number_format($total) }}</p>
</div>

<div class="form-group">
    <label for="">Pagar Depan/T: 1,5M (m)</label>
    <p class="form-control">{{ number_format($total_pagar_depan) }}</p>
</div>

<div class="form-group">
    <label for="">Pagar Belakang/T: 3M (m)</label>
    <p class="form-control">{{ number_format($total_pagar_belakang) }}</p>
</div>

<div class="form-group">
    <label for="">Pagar Samping/T: 2M (m)</label>
    <p class="form-control">{{ number_format($total_pagar_samping) }}</p>
</div>

<div class="form-group">
    <label for="">Harga Rumah (m^2) Non Standar</label>
    <p class="form-control">{{ number_format($total_non_standar) }}</p>
</div>

<div class="form-group">
    <label for="">Pagar Depan/T: 1,5M (m) Non Standar</label>
    <p class="form-control">{{ number_format($total_pagar_depan_non_standar) }}</p>
</div>

<div class="form-group">
    <label for="">Pagar Belakang/T: 3M (m) Non Standar</label>
    <p class="form-control">{{ number_format($total_pagar_belakang_non_standar) }}</p>
</div>

<div class="form-group">
    <label for="">Pagar Samping/T: 2M (m) Non Standar</label>
    <p class="form-control">{{ number_format($total_pagar_samping_non_standar) }}</p>
</div>